name: stroke-prediction-env
channels:
  - defaults
dependencies:
  - python=3.10
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - mlflow
      - dagshub
      - matplotlib
      - joblib
